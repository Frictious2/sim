<div class="ministry-card h-100">
  <div class="icon-badge"><i class="<%= item.icon %>"></i></div>
  <h3 class="h5 mt-4"><%= item.title %></h3>
  <p class="text-muted mb-0"><%= item.text %></p>
</div>
