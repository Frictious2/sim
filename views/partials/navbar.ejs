<nav class="navbar navbar-expand-lg navbar-dark site-navbar sticky-top">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center gap-2" href="/">
      <span class="brand-mark">SIM</span>
      <span class="brand-text"><%= site.brand %></span>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="mainNav">
      <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-2">
        <% site.publicNav.forEach((item) => { %>
          <li class="nav-item">
            <a class="nav-link <%= currentPath === item.href ? 'active' : '' %>" href="<%= item.href %>"><%= item.label %></a>
          </li>
        <% }) %>
        <li class="nav-item ms-lg-2"><a class="btn btn-outline-light rounded-pill px-3 btn-sm" href="/dashboard">Dashboard</a></li>
      </ul>
    </div>
  </div>
</nav>
