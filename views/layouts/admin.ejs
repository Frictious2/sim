<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
<body class="dashboard-body admin-body">
  <div class="dashboard-shell d-lg-flex">
    <aside class="dashboard-sidebar admin-sidebar"><%- include('../partials/admin-sidebar') %></aside>
    <main class="dashboard-main flex-grow-1">
      <div class="dashboard-topbar d-flex justify-content-between align-items-center gap-3">
        <div>
          <h1 class="h3 mb-1"><%= title %></h1>
          <p class="text-muted mb-0">Static admin interface for layout and content presentation.</p>
        </div>
      </div>
      <%- body %>
    </main>
  </div>
  <%- include('../partials/scripts') %>
</body>
</html>
