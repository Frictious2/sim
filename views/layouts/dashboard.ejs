<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
<body class="dashboard-body">
  <div class="dashboard-shell d-lg-flex">
    <aside class="dashboard-sidebar"><%- include('../partials/dashboard-sidebar') %></aside>
    <main class="dashboard-main flex-grow-1">
      <%- include('../partials/flash') %>
      <div class="dashboard-topbar d-flex justify-content-between align-items-center gap-3">
        <div>
          <h1 class="h3 mb-1"><%= title %></h1>
          <p class="text-muted mb-0">A simple supporter area for tracking your engagement history and current next steps.</p>
        </div>
      </div>
      <%- body %>
    </main>
  </div>
  <%- include('../partials/scripts') %>
</body>
</html>
